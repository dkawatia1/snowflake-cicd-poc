default_connection_name = "poc_conn"

[connections.poc_conn]
# Leave these as blank placeholders. 
# GitHub will overwrite them automatically via the workflow environment,
# and developers can change them locally on their own machines.
account = ""
user = ""
database = ""

